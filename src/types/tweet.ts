/**
 * A Tweet is an object with a title, description, and url.
 * @property {string} title - The title of the tweet.
 * @property {string} description - The description of the tweet.
 * @property {string} url - The URL of the tweet.
 */
export type Tweet = {
  title: string;
  description: string;
  url: string;
};
